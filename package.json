{
  "name": "teched-up-recipe-calculator",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node tools/validate_site.mjs",
    "data:import": "python tools/build_runtime.py",
    "test": "node --test tests/*.test.js",
    "start": "python -m http.server 4173 --bind 127.0.0.1 --directory dist"
  }
}
